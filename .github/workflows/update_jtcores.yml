name: update jtcores

on:
  schedule:
  - cron:  "*/20 * * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
    - name: Install apt-get utilities
      run: sudo apt-get install detox sharutils

    - uses: actions/checkout@v2
      with:
        ref: develop
  
    - name: update jtcores
      run: |
        set -euo pipefail
        git config --global user.email "theypsilon@gmail.com"
        git config --global user.name "The CI/CD Bot"
        ./.github/update_jtcores.sh . --push
      env:
        DB_ID: jtcores
        DB_URL: https://raw.githubusercontent.com/jotego/jtpremium/main/jtbindb.json.zip
        BASE_FILES_URL: https://raw.githubusercontent.com/jotego/jtpremium/%s/
        LATEST_ZIP_URL: https://github.com/jotego/jtpremium/archive/refs/heads/main.zip

